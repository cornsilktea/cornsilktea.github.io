{
  "rules": {
    "portal": {
      ".read": true,
      ".write": "auth != null && (auth.uid === 'use16pcii5RbEATuPtWhRDArmoj2' || auth.uid === 'HFcjiyXAl1YWzm3AyrZmzo4uMdG3')"
    },
    "court": {
      ".read": true,
      "now": { ".write": true },
      "room": {
        "$room": { ".write": "$room.matches(/^[A-Z0-9]{4}$/)" }
      }
    },
    "ghosts": {
      ".read": true,
      "$game": {
        "$key": {
          ".write": "$game.matches(/^[a-z]+$/) && $key.matches(/^[a-z]+$/) && newData.child('score').val() === root.child('records').child($game).child($key).child('score').val()",
          ".validate": "newData.hasChildren(['score', 'name', 'data'])",
          "score": { ".validate": "newData.isNumber()" },
          "name": { ".validate": "newData.isString() && newData.val().length >= 1 && newData.val().length <= 20" },
          "data": { ".validate": "newData.isString() && newData.val().length <= 120000" },
          "car": { ".validate": "newData.isString() && newData.val().matches(/^[a-z]+$/)" },
          "$other": { ".validate": false }
        }
      }
    },
    "records": {
      ".read": true,
      "$game": {
        "$key": {
          ".write": "$game.matches(/^[a-z]+$/) && $key.matches(/^[a-z]+$/) && (!data.exists() || newData.child('score').val() > data.child('score').val())",
          ".validate": "newData.hasChildren(['score', 'name', 'at']) && newData.child('score').isNumber() && newData.child('score').val() >= 0 && ($game !== 'slidecard' || newData.child('score').val() <= 999997500) && ($game !== 'robotmaze' || newData.child('score').val() <= 999999500) && ($game !== 'brickbreak' || newData.child('score').val() <= 500) && ($game !== 'snakegame' || newData.child('score').val() <= 399) && ($game !== 'conditionrun' || newData.child('score').val() <= 100) && ($game !== 'ramentiming' || newData.child('score').val() <= 50000) && ($game !== 'baseball' || newData.child('score').val() <= 300000) && ($game !== 'hanokroofrun' || newData.child('score').val() <= 50000) && ($game !== 'pianotiles' || newData.child('score').val() <= 400000) && ($game !== 'timeguess' || newData.child('score').val() <= 1000000000) && ($game !== 'turbolap' || newData.child('score').val() <= 999955000)",
          "score": { ".validate": "newData.isNumber()" },
          "name": { ".validate": "newData.isString() && newData.val().length >= 1 && newData.val().length <= 20" },
          "at": { ".validate": "newData.isNumber() && newData.val() > now - 600000 && newData.val() < now + 600000" },
          "grade": { ".validate": "newData.isNumber() && (newData.val() === 1 || newData.val() === 2)" },
          "cls": { ".validate": "newData.isNumber() && newData.val() >= 1 && newData.val() <= 5" },
          "$other": { ".validate": false }
        }
      }
    }
  }
}
