{
  "rules": {
    "portal": {
      ".read": true,
      ".write": "auth != null && (auth.uid === 'use16pcii5RbEATuPtWhRDArmoj2' || auth.uid === 'HFcjiyXAl1YWzm3AyrZmzo4uMdG3')"
    },
    "court": {
      ".read": true,
      "now": { ".write": true },
      "room": {
        "$room": { ".write": "$room.matches(/^[A-Z0-9]{4}$/)" }
      }
    },
    "records": {
      ".read": true,
      "pianotiles": {
        "$song": {
          ".write": "$song.matches(/^[a-z]+$/) && (!data.exists() || newData.child('score').val() > data.child('score').val())",
          ".validate": "newData.hasChildren(['score', 'name', 'at']) && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('name').isString() && newData.child('name').val().length >= 1 && newData.child('name').val().length <= 20 && newData.child('at').isNumber()"
        }
      }
    }
  }
}
