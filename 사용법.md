# 선생님용 사용법

로그인은 **선생님 구글 계정 하나**만 쓰면 됩니다. 학생은 로그인하지 않습니다.

---

## 1. 처음 한 번만 하는 준비 (약 15분)

### ① Firebase 프로젝트 만들기
1. https://console.firebase.google.com 접속 → 구글 계정으로 로그인
2. **프로젝트 만들기** → 이름은 아무거나 (예: `cornsilktea`)
3. "Google 애널리틱스"는 **사용 안 함**으로 두고 만들기

### ② 데이터 저장소 만들기
1. 왼쪽 메뉴 **빌드 → Realtime Database** → **데이터베이스 만들기**
2. 위치는 **asia-southeast1 (싱가포르)** 선택
3. **잠금 모드로 시작** 선택 → 사용 설정
4. 화면 위쪽에 나오는 주소를 복사해 둡니다. → 이게 **DB_URL**
   예: `https://cornsilktea-default-rtdb.asia-southeast1.firebasedatabase.app`

### ③ 구글 로그인 켜기
1. 왼쪽 메뉴 **빌드 → Authentication** → **시작하기**
2. **Sign-in method** 탭 → **Google** 선택 → **사용 설정** 켜기
3. "프로젝트 지원 이메일"은 내 구글 주소를 고르고 **저장**

### ④ 우리 사이트 주소를 승인 목록에 넣기  ★ 빠뜨리면 로그인이 안 됩니다
1. **Authentication → Settings(설정) 탭 → 승인된 도메인**
2. **도메인 추가** → `cornsilktea.github.io` 입력 → 추가

### ⑤ 키 두 개 더 복사하기
1. 왼쪽 위 **톱니바퀴 → 프로젝트 설정 → 일반** 맨 아래 **내 앱**
2. **웹(`</>`)** 아이콘을 눌러 앱을 하나 만듭니다 (별명은 아무거나, 호스팅 설정은 체크하지 않음)
3. 나오는 코드에서 두 값을 복사합니다.
   * `apiKey: "AIza..."` → **API_KEY**
   * `authDomain: "cornsilktea.firebaseapp.com"` → **AUTH_DOMAIN**

### ⑥ 설정 파일 채우고 올리기
`firebase-config.js` 를 열어 세 줄을 채웁니다.

```js
DB_URL:      "②에서 복사한 주소",
API_KEY:     "⑤의 apiKey",
AUTH_DOMAIN: "⑤의 authDomain",
```

저장한 뒤 **GitHub Desktop에서 커밋 → 푸시**. 1~2분 뒤 사이트에 반영됩니다.

### ⑦ 수업 자료 목록 처음 넣기
**Realtime Database → 데이터** 탭 → 오른쪽 위 **⋮ → JSON 가져오기** →
이 폴더의 `초기데이터.json` 을 선택하면 수업 자료 7개가 한 번에 등록됩니다.

### ⑧ 한 번 로그인해서 내 UID 확인하기
1. **https://cornsilktea.github.io/admin.html** 접속
2. **구글 계정으로 로그인** 누르고 내 계정 선택
3. 화면 위쪽 "지금 상태" 칸 아래에 이렇게 나옵니다.
   `로그인: 내주소@gmail.com · 내 UID: k3Jd8xxxxxxxx`
   → 이 **UID를 복사**합니다.

### ⑨ 나만 켜고 끌 수 있게 규칙 넣기
**Realtime Database → 규칙** 탭에 아래를 붙여넣고,
`여기에-내-UID-붙여넣기` 자리에 ⑧에서 복사한 UID를 넣은 뒤 **게시**하세요.

```json
{
  "rules": {
    "portal": {
      ".read": true,
      ".write": "auth != null && auth.uid === '여기에-내-UID-붙여넣기'"
    }
  }
}
```

이제 `admin.html` 로 돌아가 스위치를 켜 보세요. 정상 동작하면 준비 끝입니다.
(⑨ 전에는 스위치를 켜도 "쓰기 권한이 없습니다"가 나오는 게 정상입니다.)

---

## 2. 매 수업 사용법

1. 휴대폰이나 노트북에서 **https://cornsilktea.github.io/admin.html** 접속
2. **구글 계정으로 로그인** (한 번 로그인하면 그 기기에서는 계속 유지됩니다)
3. 오늘 쓸 수업 자료의 **스위치를 켜기** → 학생 화면에 5초 안에 열립니다
4. 수업이 끝나면 **전체 끄기** 한 번이면 정리 끝

* 자료를 열어 둔 학생도 스위치를 끄면 5초 안에 "선생님이 종료했어요" 화면으로 바뀝니다.
* 학생이 주소를 직접 입력해 들어가도 똑같이 막힙니다.
* 다른 사람이 이 주소를 알아내 구글 로그인을 해도, UID가 달라서 **아무것도 바꿀 수 없습니다.**

---

## 3. 새 수업 자료를 추가할 때

**1) HTML 파일 맨 위(`<head>` 바로 다음)에 이 두 줄을 넣습니다.**
`data-game` 값은 마음대로 정하되 영문 소문자로 적습니다.

```html
<script charset="utf-8" src="firebase-config.js"></script>
<script charset="utf-8" src="access-guard.js" data-game="새자료아이디"></script>
```

**2)** 그 파일에 `Content-Security-Policy` 라는 줄이 있다면, 그 줄 안의
`connect-src 'none';` 부분을 아래로 바꿔 주세요. (없으면 넘어가도 됩니다.)

```
connect-src https://*.firebasedatabase.app https://*.firebaseio.com;
```

**3)** GitHub Desktop으로 커밋·푸시

**4)** `admin.html` 아래쪽 **수업 자료 추가**에서 제목·파일 이름·자료 아이디를 입력하고 추가
(자료 아이디는 1)에서 정한 `data-game` 값과 **똑같아야** 합니다.)

목록에서 제목·설명 수정, 순서 바꾸기(▲▼), 삭제도 모두 이 화면에서 됩니다.
삭제해도 HTML 파일 자체는 지워지지 않고 목록에서만 빠집니다.

---

## 4. 문제가 생기면

| 화면에 나오는 말 | 해결 방법 |
|---|---|
| "이 주소가 Firebase에 등록되어 있지 않습니다" | ④번을 안 한 것입니다. 승인된 도메인에 `cornsilktea.github.io` 추가 |
| "구글 로그인이 켜져 있지 않습니다" | ③번을 안 한 것입니다 |
| "쓰기 권한이 없습니다" | ⑨번 규칙의 UID가 화면에 보이는 내 UID와 같은지 확인 |
| "로그인 도구를 불러오지 못했습니다" | 인터넷 연결 확인 후 새로고침 |
| 학생 화면에 "연결을 확인하는 중" | 학생 기기 인터넷 연결. 계속 그러면 `firebase-config.js` 의 DB_URL 확인 |
| 자료가 "아직 등록되지 않은 자료" | 수업 자료 파일의 `data-game` 값과 제어판의 자료 아이디가 같은지 확인 |
| 로그인 창이 안 뜸 | 브라우저의 팝업 차단 해제. (막히면 자동으로 페이지 이동 방식으로 다시 시도합니다) |

**급할 때**: Firebase 콘솔 → Realtime Database → 데이터 탭에서
`portal → games → (자료) → open` 값을 직접 `true`/`false`로 고쳐도 똑같이 동작합니다.

---

## 5. 요금

무료(Spark) 요금제 범위 안에서 돌아갑니다. 학생 30명이 5초마다 확인해도
하루 사용량이 무료 한도의 1%도 되지 않습니다. 카드 등록이 필요 없습니다.
